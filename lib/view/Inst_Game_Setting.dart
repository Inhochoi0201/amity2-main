import 'package:amity2/util/Import_Package.dart';
class InstGameSetting extends StatelessWidget {
  const InstGameSetting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
